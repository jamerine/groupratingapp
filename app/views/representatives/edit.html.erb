<div class="container">
  <div class="row">

<%= form_for(@representative, html: { multipart: true }) do |f| %>
  <div class="form-group">
    <%= f.label :logo %>
    <%= f.file_field :logo %>
  </div>

  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
  </div>
</div>
